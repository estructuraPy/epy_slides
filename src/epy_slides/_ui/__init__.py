"""Qt widgets and windows for epy_slides.

Holds the tab widget, every dialog, and the theme catalogue that drives
the Qt chrome built by ``app.py``. These are implementation details of
the desktop editor, not the public API — scriptable/headless use goes
through ``epy_slides.SlideDeck`` (see the package root ``__init__.py``).
"""
